{
  "name": "debian_flutter",
  "workspaceFolder": "/home/developer/workspace",
  "dockerComposeFile": [
    "docker-compose.yml"
  ],
  "service": "flutter",
  "customizations": {
    "vscode": {
      "settings": {
        "files.autoSave": "onFocusChange",
        "[dart]": {
          "editor.formatOnSave": true,
          "editor.formatOnType": true,
          "editor.rulers": [
            80
          ],
          "editor.selectionHighlight": false,
          "editor.tabCompletion": "onlySnippets",
          "editor.wordBasedSuggestions": "off"
        },
        "dart.flutterSdkPath": "/opt/flutter"
      },
      "extensions": [
        "Dart-Code.flutter",
        "Dart-Code.dart-code",
        "ms-vscode-remote.remote-containers",
        "ms-azuretools.vscode-containers",
        "GitHub.codespaces"
      ]
    }
  },
  "postCreateCommand": "echo 'export PATH=/opt/flutter/bin:$PATH' >> /home/developer/.bashrc && echo 'export DISPLAY=:1' >> /home/developer/.bashrc && echo 'pgrep Xvfb || (Xvfb :1 -screen 0 1280x720x24 &)' >> /home/developer/.bashrc && yes | /opt/flutter/bin/flutter doctor --android-licenses && /opt/flutter/bin/flutter config --no-analytics && cd my_app && flutter pub get && echo 'Flutter pub get at my_app complete'"
}